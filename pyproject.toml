[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 110
extend-exclude = ["output", "outputs", "reports"]

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = ["E501"]
